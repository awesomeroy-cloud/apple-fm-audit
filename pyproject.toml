[project]
name = "apple-fm-audit"
version = "0.1.0"
description = "Audit reverse proxy for Apple Foundation Models (fm serve)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.40.0",
]

[tool.uv]
package = false
